<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
         
<mapper namespace="com.ssafy.travelquest.domain.quest.repository.QuestRepository">
  <insert id="insertQuest" parameterType="Quest"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO quest
      (dungeon_id, attraction_id, title, description, order_index, status, created_at)
    VALUES
      (#{dungeonId}, #{attractionId}, #{title}, #{description}, #{orderIndex}, #{status}, NOW())
  </insert>
</mapper>