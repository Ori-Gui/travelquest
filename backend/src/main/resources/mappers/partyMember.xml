<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.travelquest.domain.party.repository.PartyMemberRepository">

    <!-- ResultMap -->
    <resultMap id="partyMemberResultMap" type="com.ssafy.travelquest.domain.party.entity.PartyMember">
        <id property="id" column="id" />
        <result property="partyId" column="party_id" />
        <result property="userId" column="user_id" />
        <result property="jobCode" column="job_code" javaType="com.ssafy.travelquest.domain.user.entity.JobCode" />
        <result property="joinedAt" column="joined_at" />
    </resultMap>

    <!-- INSERT -->
    <insert id="insertPartyMember" parameterType="com.ssafy.travelquest.domain.party.entity.PartyMember"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO party_member (party_id, user_id, job_code, joined_at)
        VALUES (#{partyId}, #{userId}, #{jobCode}, #{joinedAt})
    </insert>

    <!-- DELETE by partyId and userId -->
    <delete id="deletePartyMember">
        DELETE FROM party_member
        WHERE party_id = #{partyId}
          AND user_id = #{userId}
    </delete>

    <!-- DELETE by userId -->
    <delete id="deletePartyMemberByUserId">
        DELETE FROM party_member
        WHERE user_id = #{userId}
    </delete>

    <!-- SELECT 전체 PartyMember 리스트 -->
    <select id="getPartyMembersByPartyId" resultMap="partyMemberResultMap">
        SELECT id, party_id, user_id, job_code, joined_at
        FROM party_member
        WHERE party_id = #{partyId}
    </select>

    <select id="findMembersByPartyId" parameterType="long" resultType="com.ssafy.travelquest.domain.party.dto.PartyMemberDetailResponse">
        SELECT
            u.id AS id,
            u.user_name AS nickname,
            u.job_class_code AS job,
            jc.name AS job_name,
            u.mbti AS mbti,
            CASE WHEN u.id = p.leader_id THEN TRUE ELSE FALSE END AS is_leader
        FROM party_member pm
                 JOIN user u ON pm.user_id = u.id
                 JOIN job_class jc ON u.job_class_code = jc.code
                 JOIN party p ON pm.party_id = p.id
        WHERE pm.party_id = #{partyId}
    </select>

    <select id="findByUserAndParty"
            parameterType="map"
            resultMap="partyMemberResultMap">
        SELECT
            id,
            party_id,
            user_id,
            job_code,
            joined_at
        FROM party_member
        WHERE user_id  = #{userId}
          AND party_id = #{partyId}
    </select>

    <select id="findByUserId" resultMap="partyMemberResultMap">
        SELECT id, party_id, user_id, job_code, joined_at
        FROM party_member
        WHERE user_id = #{userId}
    </select>
    
    <!-- 결과 매핑 -->
    <resultMap id="partyMemberWithUserResultMap" type="com.ssafy.travelquest.domain.party.dto.PartyMemberInfo">
        <id property="id" column="id"/>
        <result property="partyId" column="party_id"/>
        <result property="userId" column="user_id"/>
        <result property="jobClassCode" column="job_class_code"/>
        <result property="profileImage" column="profile_image"/>
        <result property="joinedAt" column="joined_at"/>
    </resultMap>

    <!-- 조회 쿼리 -->
    <select id="findPartyMembersWithUser" parameterType="long" resultMap="partyMemberWithUserResultMap">
        SELECT pm.id AS id,
               pm.party_id AS party_id,
               pm.user_id AS user_id,
               pm.job_code AS job_class_code,
               u.profile_image AS profile_image,
               pm.joined_at AS joined_at
        FROM party_member pm
        LEFT JOIN user u ON pm.user_id = u.id
        WHERE pm.party_id = #{partyId}
    </select>
</mapper>
